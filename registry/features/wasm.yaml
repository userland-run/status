# SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-UEL
# Copyright (C) 2026 And The Next GmbH - https://userland.run
#
# The WASM tier (nano/nodert/src/wasm) — wasm32-wasip1 apps as Kernel
# processes. specs/nano/wasm-tier (UL-SPEC/wasm-tier) W-1.

- { id: wasm.wasip1.run, title: "run wasm32-wasip1 modules as kind:wasm processes", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §4" }
- { id: wasm.wasi.shim, title: "wasi_snapshot_preview1 shim over the Syscall Bus", area: wasm, status: partial, since: "2026-07", notes: "core syscalls (args/env/fd/path/clock/random/exit); fd_readdir + more in W-2" }
- { id: wasm.caps.preopens, title: "structural preopen capabilities (caps.fs.scopes → preopens)", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §5 P1/P2" }
- { id: wasm.preopen.escape, title: "preopen-escape denial ('..' traversal → ENOTCAPABLE)", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier W2" }
- { id: wasm.spawn, title: ".wasm PATH routing + cross-tier spawn (node → wasm)", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §6 T1" }
- { id: wasm.stdio, title: "stdio via Kernel pipes (pipeline participation)", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier T2" }
- { id: wasm.threads, title: "wasip1-threads (wasi_thread_spawn, shared SAB memory)", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §4.1 X4" }
- { id: wasm.inspect, title: "wasm inspect — imports/exports/memory/threads/wasi-version", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §7 M2" }
- { id: wasm.cache, title: "compiled-Module cache keyed by content hash", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §4.1 X5" }
- { id: wasm.stats, title: "syscall counters + running-instance stats", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §7 M3" }
- { id: wasm.service.runner, title: "WASI service runner — wasm module as a svc.* Kernel Service", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §9 (W-3)", notes: "createWasiService + registerWasmServiceFromManifest: a wasip1 module runs per-invoke as a request→stdin→wasm→stdout→response filter, reachable over the svc.* bus (list/invoke); catalog kind:wasm-service manifest drives registration. runWasm gained stdinBytes. 6/6 headless (stdin-echo fixture). SDK install auto-routing DONE (Nano.installApp auto-registers wasm-services). Persistent-instance (SWC-style, warm) variant pending" }
- { id: wasm.service.install, title: "SDK install auto-routing — Nano.installApp registers a wasm-service", area: wasm, status: shipped, level: tested, since: "2026-07", spec: "wasm-tier §9 (W-3)", notes: "installApp auto-registers a kind:wasm-service with the Kernel service registry via the vendored runner (Nano.registerWasmService); wasm-apps untouched. 3/3 headless (dist Nano + real Kernel + Map-backed fs)" }
- { id: wasm.http, title: "wasi-http components behind ServeBridge", area: wasm, status: planned, spec: "wasm-tier §8 (W-3)" }
- { id: wasm.catalog, title: "catalog wasm-app / wasm-component artifact kinds", area: wasm, status: partial, level: tested, since: "2026-07", spec: "wasm-tier §9 (W-2/W-3)", notes: "Manifest.kind discriminator (elf/wasm-app/service/component) + package.mjs magic-sniff + wasm32-wasip1 abi + manifestKind()/isWasmKind() helpers shipped & tested (both sign/verify sides); wasm-app installs onto PATH (.wasm router runs it); wasm-service registers with the service registry via the WASI service runner (wasm.service.runner). SDK install auto-routing + wasm-component→ServeBridge pending (W-3)" }
